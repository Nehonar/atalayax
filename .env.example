POSTGRES_DB=atalayax
POSTGRES_USER=atalayax
POSTGRES_PASSWORD=atalayax_dev_password

SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=

BACKEND_PORT=4000
FRONTEND_PORT=3000
API_URL=http://backend:4000/api
NEXT_PUBLIC_API_URL=http://localhost:4000/api
DATABASE_URL=postgresql://atalayax:atalayax_dev_password@postgres:5432/atalayax
SESSION_SECRET=replace-with-a-long-random-secret

# MinIO (opcional) — cuando MINIO_ENDPOINT está definido, los archivos de demo se suben al bucket
# MINIO_ENDPOINT=http://minio:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=sensor-demo-uploads
